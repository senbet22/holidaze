/**
 * Context for authentication state and methods.
 * Provides user info, login, logout, etc. throughout the app.
 */

import { createContext } from "react";

export const AuthContext = createContext();
